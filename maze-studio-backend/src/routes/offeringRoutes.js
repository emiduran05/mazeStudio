const express=require("express");const auth=require("../middlewares/authMiddleware");const educator=require("../middlewares/educatorOnlyMiddleware");const c=require("../controllers/offeringController");const router=express.Router();router.get("/learning-journeys/:journeyId/offerings",auth,educator,c.list);router.post("/learning-journeys/:journeyId/offerings",auth,educator,c.create);router.put("/offerings/:offeringId",auth,educator,c.update);router.delete("/offerings/:offeringId",auth,educator,c.remove);module.exports=router;
