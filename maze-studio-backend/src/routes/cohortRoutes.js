const express=require("express"),auth=require("../middlewares/authMiddleware"),educator=require("../middlewares/educatorOnlyMiddleware"),c=require("../controllers/cohortController");const router=express.Router();router.get("/offerings/:offeringId/cohorts",auth,educator,c.list);router.post("/offerings/:offeringId/cohorts",auth,educator,c.create);router.put("/cohorts/:cohortId",auth,educator,c.update);router.get("/cohorts/:cohortId/members",auth,educator,c.members);router.post("/cohorts/:cohortId/members",auth,educator,c.add);router.delete("/cohorts/:cohortId/members/:enrollmentId",auth,educator,c.remove);module.exports=router;
