const express=require("express");const auth=require("../middlewares/authMiddleware");const c=require("../controllers/notificationController");const router=express.Router();router.get("/notifications",auth,c.list);router.patch("/notifications/read-all",auth,c.markAll);router.patch("/notifications/:id/read",auth,c.markRead);router.get("/notification-preferences",auth,c.preferences);router.put("/notification-preferences",auth,c.updatePreferences);module.exports=router;
