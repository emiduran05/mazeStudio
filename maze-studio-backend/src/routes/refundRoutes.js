const r=require("express").Router(),auth=require("../middlewares/authMiddleware"),educator=require("../middlewares/educatorOnlyMiddleware"),c=require("../controllers/refundController");r.post("/refund-requests",auth,c.request);r.get("/refund-requests",auth,educator,c.list);r.post("/refund-requests/:id/approve",auth,educator,c.approve);r.post("/refund-requests/:id/reject",auth,educator,c.reject);module.exports=r;
