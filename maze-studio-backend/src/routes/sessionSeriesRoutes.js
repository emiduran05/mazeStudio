const express=require("express"),auth=require("../middlewares/authMiddleware"),educator=require("../middlewares/educatorOnlyMiddleware"),c=require("../controllers/sessionSeriesController");const router=express.Router();router.get("/cohorts/:cohortId/session-series",auth,educator,c.list);router.post("/cohorts/:cohortId/session-series",auth,educator,c.create);router.get("/session-series/:seriesId/events",auth,educator,c.events);router.patch("/calendar/events/:eventId/scope",auth,educator,c.update);module.exports=router;
