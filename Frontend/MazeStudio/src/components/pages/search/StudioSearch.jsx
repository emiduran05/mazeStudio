import {useEffect,useState} from "react";import {Link,useSearchParams} from "react-router-dom";import StudioLayout from "../../layouts/studioLayout/StudioLayout";import {apiRequest} from "../../../api/api";import "./StudioSearch.css";
const icons={JOURNEY:"fa-route",STUDENT:"fa-user-graduate",CHALLENGE:"fa-list-check"};
export default function StudioSearch(){const[params]=useSearchParams(),query=params.get("q")||"",[results,setResults]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{setLoading(true);apiRequest(`/search?q=${encodeURIComponent(query)}`).then(data=>setResults(data.results||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[query]);return <StudioLayout><main className="studio_search_page"><header><span>GLOBAL SEARCH</span><h1>Results for “{query}”</h1><p>Learning Journeys, learners and Challenges in your Studio.</p></header>{error&&<div className="studio_search_empty">{error}</div>}{loading?<div className="studio_search_empty">Searching…</div>:results.length?<section>{results.map(item=><Link to={item.action_url} key={`${item.type}-${item.id}`}><i className={`fa-solid ${icons[item.type]}`}/><span><small>{item.type}</small><strong>{item.label}</strong><p>{item.description}</p></span><i className="fa-solid fa-arrow-right"/></Link>)}</section>:<div className="studio_search_empty"><i className="fa-solid fa-magnifying-glass"/><h2>No results found</h2><p>Try a learner name, course title or Challenge.</p></div>}</main></StudioLayout>}
